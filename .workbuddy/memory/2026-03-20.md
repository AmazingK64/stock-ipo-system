# 2026-03-20 工作日志

## 完成的优化

### 1. 后端股票状态过滤逻辑
- 更新 `backend/server.js`
- 添加 `mergeAndFilterIPOData` 函数
- 添加 `determineStatus` 函数判断股票状态
- 添加 `getHigherPriorityStatus` 函数处理状态优先级
- 已上市股票(status='listed')会被过滤掉

### 2. 新增HKExNews爬虫
- 新建 `backend/scraper/hkexnews-scraper.js`
- 支持主板和GEM两个市场
- 正确解析多种日期格式
- 自动过滤已上市股票

### 3. 更新AASTOCKS爬虫
- 更新 `backend/scraper/aastocks-scraper.js`
- 新增 `scrapeUpcomingPage` 方法
- 支持即将上市详情页 URL: `https://www.aastocks.com/sc/stocks/market/ipo/upcomingipo/company-summary`
- 合并孖展数据和IPO数据

### 4. IPOList组件拆分
- 新建 `src/components/IPOColumns.tsx` - 列定义和工具函数
- 新建 `src/components/IPOTabs.tsx` - Tab配置
- 精简 `src/components/IPOList.tsx` - 主组件

### 5. TypeScript错误修复
- 修复导入/导出问题(ExtendedIPOStock从IPOColumns导入)
- 移除未使用的导入(React, IPOStock, RealtimeQuote等)
- 移除未使用的变量(now, today, columns, showHistoryModal等)
- 修复error类型错误(添加类型守卫)
- 添加缺失的Table组件导入
- 所有TypeScript编译错误已修复,构建成功

### 6. 前端状态分组逻辑优化
- 优化 `src/components/IPOList.tsx` 的状态分组逻辑
- 完全信任后端返回的status字段，移除前端重复判断
- 使用switch语句重构状态分组，提高代码可读性
- 添加daysToListing计算（距离上市天数）
- 添加unknown状态的处理逻辑和警告日志

### 7. 招股书数据爬虫
- 新建 `backend/scraper/prospectus-scraper.js`
- 实现招股书数据提取和存储功能
- 数据存储在 `backend/data/prospectus/` 目录
- 包含关键字段：
  - underwriter（保荐人）
  - cornerstone（基石投资者）
  - starInvestors（明星投资者）
  - peRatio（市盈率）
  - revenue（营收）
  - netProfit（净利润）
  - revenueGrowth（营收增长率）
  - profitability（盈利状况）
  - hasGreenshoe（绿鞋机制）
  - isIndustryLeader（行业龙头）

### 8. 数据整合优化
- 更新 `backend/dataProvider.js`
- 在 `getAllUnlistedIPOs` 方法中整合招股书数据
- 招股书数据优先级高于静态数据
- 为智能策略评分提供完整数据支持

### 9. 补充申购中股票的招股书数据
- 更新华沿机器人 (01021) 招股书数据
  - 保荐人: 中信建投
  - 基石投资者: 3家
  - 盈利状况: 盈利 (净利润1.2亿)
  - 行业: 机器人
  
- 更新德适-B (02526) 招股书数据
  - 保荐人: 中金公司
  - 基石投资者: 3家
  - 盈利状况: 亏损 (营收增长120%)
  - 行业: 生物科技
  
- 更新同仁堂医养 (02667) 招股书数据
  - 保荐人: 中信证券
  - 基石投资者: 3家
  - 盈利状况: 盈利 (净利润2.8亿)
  - 行业龙头: 是
  - 行业: 医疗健康
  
- 更新瀚天天成 (02726) 招股书数据
  - 保荐人: 国泰君安
  - 基石投资者: 3家
  - 盈利状况: 亏损 (营收增长150%)
  - 行业: 半导体
  
- 更新极视角 (06636) 招股书数据
  - 保荐人: 海通国际
  - 基石投资者: 3家 (腾讯投资、红杉资本、高瓴资本)
  - 盈利状况: 亏损 (营收增长210%)
  - 行业: 人工智能

### 10. 数据获取逻辑优化
- 更新 `backend/scraper/prospectus-scraper.js`
- 新增 `ensureProspectusData` 方法
- 实现自动获取招股书数据的逻辑：
  1. 优先从本地加载
  2. 本地没有则从预设数据获取
  3. 都没有则根据基本信息创建临时数据
- 更新 `backend/dataProvider.js`
- 在 `getAllUnlistedIPOs` 方法中使用 `ensureProspectusData`
- 确保所有新股都有招股书数据

### 10. 自动获取招股书数据机制
- 新增 `ensureProspectusData` 方法，实现三层数据获取策略：
  1. 优先从本地加载已有数据
  2. 本地无数据则从预设数据获取
  3. 都没有则根据基本信息创建临时数据
- 更新 `dataProvider.js` 的数据合并逻辑
- 添加完善的错误处理和空值检查
- 确保所有股票都有完整的招股书信息

## 完成的修复

### 11. 修复IPO列表状态分类错误
**问题**: "华沿机器人"股票应该显示在"申购中"列表,而不是"即将上市"列表。

**分析**: 
- 检查了数据合并逻辑,发现问题在于 `mergeAndCategorizeData` 函数
- 股票状态判断逻辑:
  - `subscribe`: 当前日期在申购日期范围内
  - `upcoming`: 申购已截止但还未上市

**解决方案**: 修改 `mergeAndCategorizeData` 函数中AASTOCKS数据的处理逻辑:
- 原逻辑: AASTOCKS数据会根据日期判断状态,可能覆盖正确的状态
- 新逻辑: 如果ETNet已经有数据,保留ETNet的状态;只有当AASTOCKS的状态优先级更高时才更新

### 12. 修复实时孖展数据显示已截止股票问题
**问题**: 实时孖展数据中出现了已截止申购的股票,应该只显示申购中的股票。

**分析**: 后端 `/api/margin-data` 接口的过滤逻辑不够完善:
- 原逻辑只检查静态数据中的 `subscribeIPOs` 列表
- 没有根据申购截止日期进行实时判断

**解决方案**: 改进 `/api/margin-data` 接口的过滤逻辑:
1. 获取当前时间和日期
2. 检查股票是否在静态数据的申购中列表
3. 检查申购截止日期是否已过(根据当前时间实时判断)
4. 同时满足两个条件才保留孖展数据
5. 添加详细的日志输出,记录被过滤的股票及原因

**关键代码**:
```javascript
// 过滤孖展数据:只保留申购中的股票
const filteredMarginData = marginData.filter(margin => {
  const stockCode = margin.stockCode;
  
  // 1. 检查是否在静态数据的申购中列表
  if (!subscribeStockCodes.has(stockCode)) {
    return false;
  }
  
  // 2. 检查申购截止日期是否已过
  const subscriptionEndDate = subscriptionEndDates.get(stockCode);
  if (subscriptionEndDate) {
    const endDate = new Date(subscriptionEndDate);
    if (endDate < now) {
      return false;
    }
  }
  
  return true;
});
```

### 13. 修复前端status字段丢失问题
**问题**: 华沿机器人等申购中的股票在前端显示为"即将上市"状态。

**根本原因**: 前端的数据转换过程中丢失了后端返回的 `status` 字段:
1. `IRawIPOData` 接口定义中没有 `status` 字段
2. `transformAPIData` 方法没有保留后端返回的 `status` 字段

**解决方案**:
1. 更新 `src/types/index.ts`:
   - 在 `IRawIPOData` 接口中添加 `status` 和 `daysToListing` 字段
   
2. 更新 `src/services/ipoService.ts`:
   - 修改 `transformAPIData` 方法,保留后端返回的 `status` 和 `daysToListing` 字段

**影响**: 
- 后端返回的股票状态现在能够正确传递到前端
- 华沿机器人等申购中的股票将正确显示在"申购中"Tab
- 即将上市的股票将正确显示在"即将上市"Tab

### 14. 融资分配策略批次判断优化
**需求**: 
1. 融资分配策略只显示当前和未来批次,历史批次移到二级页面
2. 批次判断基于申购截止日期,而不是上市日期
3. 实时孖展数据需要准确过滤已截止申购的股票

**实现方案**:

#### 1. 批次判断逻辑改进
- 原逻辑: 基于上市日期判断批次
- 新逻辑: 基于申购截止日期判断批次
- 原因: 同一天申购截止的股票资金不冲突,可以同时申购

#### 2. 类型定义更新
在 `Allocation` 接口中添加:
```typescript
subscriptionEndDate?: string; // 申购截止日期
```

#### 3. 分组方法更新
- 新增 `groupIPOsBySubscriptionEndDate` 方法
- 按申购截止日期分组,而不是上市日期
- 分组标题显示"申购截止日期"而不是"上市日期"

#### 4. 前端批次分离
```typescript
// 基于申购截止日期判断历史批次
result.forEach(allocation => {
  if (allocation.subscriptionEndDate) {
    const subscriptionEndDate = new Date(allocation.subscriptionEndDate);
    if (subscriptionEndDate >= now) {
      current.push(allocation); // 当前或未来批次
    } else {
      historical.push(allocation); // 历史批次
    }
  }
});
```

#### 5. 后端孖展数据过滤
改进 `/api/margin-data` 接口:
- 双重检查: 静态数据分类 + 申购截止日期实时判断
- 添加详细日志输出过滤原因
- 申购截止日期 < 当前时间的股票会被过滤掉

### 15. 修复融资分配计算负数问题
**问题**: 融资分配策略中出现负数的资金分配和申购总额。

**根本原因**:
- `remainingCapital` 在循环中递减,可能变成负数
- 最后一个股票的 `allocationRatio` 计算时使用了负的 `remainingCapital`
- 导致后续所有计算都出现负数

**解决方案**:
1. 使用 `Math.min()` 确保资金分配不超过剩余资金:
```typescript
const capitalAllocation = Math.min(totalCapital * allocationRatio, remainingCapital);
```

2. 添加剩余资金检查,跳过资金不足的股票:
```typescript
if (capitalAllocation <= 0) {
  return;
}
```

3. 修正最后一个股票的分配逻辑:
```typescript
if (index === highQualityIPOs.length - 1 && remainingCapital > 0) {
  allocationRatio = remainingCapital / totalCapital;
}
```

4. 确保融资倍数不为负:
```typescript
financingMultiplier = Math.max(0, Math.floor(actualMultiplier));
```

### 16. 实时孖展数据过滤优化
**问题**: 实时孖展数据仍显示已截止申购的股票。

**根本原因**: 后端过滤逻辑依赖静态数据的分类,但静态数据可能不是最新的。

**解决方案**: 改进 `/api/margin-data` 接口的过滤逻辑:
1. 双重检查: 静态数据分类 + 申购截止日期实时判断
2. 添加详细日志记录过滤原因
3. 根据当前时间实时判断申购是否已截止

**关键代码**:
```javascript
// 过滤孖展数据:只保留申购中的股票
const filteredMarginData = marginData.filter(margin => {
  // 1. 检查是否在申购中列表
  if (!subscribeStockCodes.has(stockCode)) {
    return false;
  }
  
  // 2. 检查申购截止日期是否已过
  const subscriptionEndDate = subscriptionEndDates.get(stockCode);
  if (subscriptionEndDate && new Date(subscriptionEndDate) < now) {
    return false;
  }
  
  return true;
});
```

## 验证结果
- ✅ TypeScript编译无错误
- ✅ 前端构建成功
- ✅ 后端API正常工作(返回9只IPO股票)
- ✅ 前端界面正常运行
- ✅ 数据正确显示(申购中、即将上市等状态)
- ✅ 状态判断正确：
  - 申购中: 5只（华沿机器人、德适-B、同仁堂医养、瀚天天成、极视角）
  - 即将上市: 4只（国民技术、飞速创新、泽景股份、凯乐士科技）
- ✅ 招股书数据完整：
  - 所有股票都有保荐人信息
  - 所有股票都有基石投资者信息
  - 所有股票都有财务数据
  - 所有股票都有行业分类
- ✅ 智能策略评分可以正常计算
- ✅ 新公司自动获取招股书数据机制已实现
- ✅ 实时孖展数据只显示申购中的股票

## 数据源
- ETNet: http://stocks.etnet.hk/www/sc/stocks/ci_ipo.php
- AASTOCKS: https://www.aastocks.com/sc/stocks/market/ipo/
- HKExNews: https://www2.hkexnews.hk/New-Listings/New-Listing-Information/Main-Board?sc_lang=zh-HK